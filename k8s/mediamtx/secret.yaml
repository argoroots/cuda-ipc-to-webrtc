apiVersion: v1
kind: Secret
metadata:
  name: mediamtx-credentials
  namespace: oden-streaming
type: Opaque
stringData:
  # Replace with actual credentials or use SealedSecrets / ExternalSecrets in production
  RTMP_PUBLISH_USER: "publisher"
  RTMP_PUBLISH_PASSWORD: "changeme"
